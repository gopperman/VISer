// Set the default dimensions and margins of the graph
const margin = {top: 20, right: 20, bottom: 30, left: 50},
    defaultWidth = 960 - margin.left - margin.right,
    defaultHeight = 500 - margin.top - margin.bottom;

const dimensions = { margin, defaultWidth, defaultHeight }

export { dimensions }